name: CI
on: [pull_request]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run lint
        run: |
          if grep -rn "FIXME-BROKEN" README.md; then echo "lint: remove the FIXME-BROKEN marker"; exit 1; fi
